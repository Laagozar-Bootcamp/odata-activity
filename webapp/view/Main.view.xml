<mvc:View controllerName="sapips.training.odata.controller.Main"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core = "sap.ui.core"
    xmlns="sap.m">
    <Page id="page" title="{i18n>title}">
    <Table id="tabProducts" inset="false" 
        items="{ path: '/Products',
                 sorter: { path: 'ProductName'} }">
        <headerToolbar>
            <OverflowToolbar id="hdrToolbar">
                <Title id="hdrIdProd" text="{i18n>prodsTitle}"></Title>
            </OverflowToolbar>
        </headerToolbar>
        <infoToolbar>
            <OverflowToolbar id="infoToolbar">
                <Label id="infoTbLbl" text="{i18n>infoText}"/>
            </OverflowToolbar>
        </infoToolbar>
        <columns>
            <Column id="idColProd">
            <Text id="idTxtProd" text="{i18n>prodCol}"/>
            </Column>

            <Column id="idColSupp">
            <Text id="idTxtSupp" text="{i18n>suppCol}"/>
            </Column>

            <Column id="idColPrice">
            <Text id="idTxtPrice" text="{i18n>priceCol}"/>
            </Column>
        </columns>
        <items>
            <ColumnListItem id="colListProd" vAlign="Middle">
            <cells>
                <ObjectIdentifier 
                    id="objProdName"
                    title="{ProductName}"
                    text="{i18n>prodIdTxt} {ProductID}"/>

                <Text id="txtSupp" text="{SupplierID}"/>
                <!--just for visualization, add Discontinued property
                <ObjectIdentifier 
                    id="objSupplier"
                    title="{SupplierID}"
                    text="Discontinued: {Discontinued}"/>-->

                <ObjectIdentifier 
                    id="objUnitPrice" 
                    title="{UnitPrice}"
                    text="{QuantityPerUnit}"/>
            </cells>
            </ColumnListItem>
        </items>
    </Table>
    </Page>
</mvc:View>